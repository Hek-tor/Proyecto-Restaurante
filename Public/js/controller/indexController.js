const downloadMenu = document.querySelector('.descargaMenu');

downloadMenu.addEventListener('click', function () {
    successMsg('Descarga exitosa', 'Se guardó correctamente el menú en formato PDF.');
});
